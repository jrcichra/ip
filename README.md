# ip
Gets your IP with no fluff

# Usage
+ `docker-compose up -d`
+ Visit port 8888 on your system

# Features
+ `/` - HTML view
+ `/ip` - plaintext ip
+ `/isp` - plaintext isp
+ Redis cache for ISP associated with IP